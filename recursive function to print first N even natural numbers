#include<stdio.h>
int even_natural(int);
int main(){
    int num,n;

printf("enter your number: ");
scanf("%d",&num);
even_natural(num);

return 0;
}
int even_natural(int num)
{
int i;
for(i=2;i<=num;i++){
    if(i%2==0)
        printf("%d\n",i);

}
}
